import 'package:flutter/material.dart';

class GenshinLFGApp extends StatelessWidget {
  const GenshinLFGApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
